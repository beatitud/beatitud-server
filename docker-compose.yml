version: '2.2'

services:
  nginx:
    restart: always
    build: ./nginx/
    ports:
    - "80:80"
    - "443:443"
    environment:
    - NGINX_HOST=TO_BE_OVERWRITTEN # Host name
    command: /bin/sh -c "envsubst '$${NGINX_HOST}' < /etc/nginx/nginx.conf.template > /etc/nginx/nginx.conf && exec nginx -g 'daemon off;'"
    volumes:
    - web:/data/web
    - ./nginx/nginx.conf.template:/etc/nginx/nginx.conf.template:ro
    - ./nginx/logs:/etc/nginx/logs
 
  django:
    extends:
      file: ./back/docker-compose.yml
      service: django
    environment:
    - LETSENCRYPT_HOST=api.beatitud.io
    - LETSENCRYPT_EMAIL=kariuki.kapombe@gmail.com
    - VIRTUAL_HOST=api.beatitud.io
    - VIRTUAL_PORT=8000
    hostname: api.beatitud.io

  showcase:
    extends:
      file: ./showcase/docker-compose.yml
      service: showcase
    environment:
    - LETSENCRYPT_HOST=www.beatitud.io
    - LETSENCRYPT_EMAIL=kariuki.kapombe@gmail.com
    - VIRTUAL_HOST=www.beatitud.io
    - VIRTUAL_PORT=4000
    hostname: www.beatitud.io

  metricbeat:
    restart: always
    container_name: metricbeat
    image: docker.elastic.co/beats/metricbeat:6.5.2
    environment:
    - ELASTICSEARCH_HOST=search-beatitud-pxtyvfcgwra3pwfl34qhw4bejm.eu-west-3.es.amazonaws.com
    - KIBANA_HOST=search-beatitud-pxtyvfcgwra3pwfl34qhw4bejm.eu-west-3.es.amazonaws.com/_plugin/kibana
    - ELASTICSEARCH_USERNAME=""
    - ELASTICSEARCH_PASSWORD=""
    # disable strict permission checks
    command: ["--strict.perms=false", "-system.hostfs=/hostfs"]
    ports:
    - "333:333"

volumes:
  nginx:
  django:
  showcase:
  metricbeat:
  web:
